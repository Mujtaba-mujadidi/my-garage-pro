NEXT_PUBLIC_APP_ENV=development
# Local only — do not point at Railway here (use localhost:4000).
API_URL=http://localhost:4000

# Web port: local dev uses 3011 (see scripts/dev.sh). Railway sets PORT automatically.
# PORT=3011
